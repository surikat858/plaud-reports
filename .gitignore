# Скомпилированные exe (собираются локально через build/ps2exe, в репозиторий не кладём)
*.exe

# Если в будущем вынесете SMTP-настройки в отдельный файл (см. "Дальнейшее развитие") -
# не забудьте добавить его сюда, чтобы не закоммитить реальный пароль:
config.local.ps1
config.local.json
*.local.ps1

# Windows / редакторы
Thumbs.db
Desktop.ini
.vscode/
.vs/
